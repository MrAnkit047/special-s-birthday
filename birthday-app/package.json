{  "name": "birthday-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "live-server --port=8080",
    "start": "live-server --port=8080"
  },
  "dependencies": {},
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
